/*

var start = 0;

while (start <= 10) {
    console.log(start);
    start++;
}

for (start ; finish ; update) {
    code block
}

*/

//for (var start = 0 ; start <= 10 ; start = start + 2) {
for (var start = 0 ; start <= 10 ; start++) {
    
    console.log(start);
   // start = start + 2;
}
